{
  "name": "gih-outlets-cover-report",
  "version": "1.0.0",
  "private": true,
  "description": "Outlets cover report: Opera import, shared day state and a GIH template builder, with an admin-controlled backend.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node server/server.js",
    "start": "node server/server.js",
    "build": "node -e \"console.log('Build complete')\"",
    "verify": "node server/verify.js"
  },
  "dependencies": {
    "@google-cloud/firestore": "^9.0.0",
    "firebase": "^12.18.0"
  }
}
